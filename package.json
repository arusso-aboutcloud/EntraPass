{
  "name": "entrapass",
  "private": true,
  "version": "0.1.0",
  "description": "Passkey Migration Assistant for Microsoft Entra ID",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx wrangler pages deploy dist --project-name=entrapass"
  },
  "dependencies": {
    "@azure/msal-browser": "^5.10.1"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^4.91.0"
  }
}
